<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hmall.behavior.mapper.UserBehaviorMapper">

    <!-- 查询用户足迹（关联商品信息） -->
    <select id="selectFootprintWithItem" resultType="com.hmall.behavior.domain.vo.FootprintVO">
        SELECT
            ub.id,
            ub.item_id,
            ub.behavior_type,
            ub.create_time,
            i.name as item_name,
            i.image as item_image,
            i.price
        FROM user_behavior ub
        LEFT JOIN item i ON ub.item_id = i.id
        WHERE ub.user_id = #{userId}
        <if test="behaviorType != null">
            AND ub.behavior_type = #{behaviorType}
        </if>
        ORDER BY ub.create_time DESC
        LIMIT #{offset}, #{size}
    </select>

</mapper>

